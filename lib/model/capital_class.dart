class CapitalDataModel {
  final String description;

  final int qty;
  final int rate;
  final String per;
  final int total;
  CapitalDataModel(this.description, this.qty, this.rate, this.per, this.total);
}

class DCWDataModel {
  final String description;

  final int qty;
  final int rate;
  final String per;
  final int total;
  DCWDataModel(this.description, this.qty, this.rate, this.per, this.total);
}
